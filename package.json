{
  "type": "module",
  "scripts": {
    "build": "node scripts/generate.js",
    "dev": "nodemon --verbose"
  },
  "dependencies": {
    "cheerio": "^1.1.1",
    "xlsx": "https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  },
  "nodemonConfig": {
    "watch": [
      "scripts",
      "src"
    ],
    "exec": "npm run build"
  }
}
